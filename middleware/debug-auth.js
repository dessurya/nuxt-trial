export default async function({ $auth, store, redirect }) {
    // console.log({ log_state_auth_debug: $auth.state.user})
    // if (store.state.auth.loggedIn) {
    //     return redirect('/')
    // }
}